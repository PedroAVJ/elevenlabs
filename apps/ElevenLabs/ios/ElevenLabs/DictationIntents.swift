// Intentionally empty.
//
// Dictation no longer publishes App Shortcuts. The Control Widget is the only
// idle start surface; the expanded session Live Activity owns pause/resume and
// cancel, and the keyboard owns Send.
